# js/firebase-config.js et js/tomtom-config.js sont commités volontairement :
# ce sont des clés client (pas des secrets serveur), protégées par restriction
# de domaine (Google Cloud Console / TomTom dashboard) et par les règles
# Firestore, pas par leur confidentialité. Nécessaire pour que le déploiement
# statique (GitHub Pages) fonctionne sans étape de build.

# Artefacts locaux Firebase CLI
firebase-debug.log
.firebase/